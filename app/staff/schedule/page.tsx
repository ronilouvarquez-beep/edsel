import { WorkspacePlaceholder } from "@/components/workspace-placeholder"

export default function StaffSchedulePage() {
  return <WorkspacePlaceholder title="Staff schedule" description="Review upcoming events, delivery times, and assigned service shifts." />
}
