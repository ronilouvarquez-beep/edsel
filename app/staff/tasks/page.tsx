import { WorkspacePlaceholder } from "@/components/workspace-placeholder"

export default function StaffTasksPage() {
  return <WorkspacePlaceholder title="Preparation tasks" description="Track ingredients, cake preparation, packing, and event setup tasks." />
}
